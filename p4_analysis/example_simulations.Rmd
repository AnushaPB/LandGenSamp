---
title: "example_simulations"
output: html_document
date: "2022-09-27"
---
```{r}
library("raster")
library("here")
```


```{r}

#Get geospatial data
get_gsd <- function(filepath){
  gsd_df <- read.csv(filepath)
  #assign IDs to rownames
  rownames(gsd_df) <- gsd_df$idx
  #extract env values
  gsd_df$env1 <- as.numeric(stringr::str_extract(gsd_df$e, '(?<=, )[^,]+(?=,)')) 
  gsd_df$env2 <- as.numeric(stringr::str_extract(gsd_df$e, '(?<=, )[^,]+(?=\\])')) 
  #extract trait values (figure out the regex way to do this later)
  #remove brackets
  z <- gsub("\\[|\\]", "", gsd_df$z)
  #split on comma
  z <- stringr::str_split_fixed(z, ", ", n=2)
  #change to numeric
  z <- apply(z, 2, as.numeric)
  #add back to df
  gsd_df$z1 <- z[,1]
  gsd_df$z2 <- z[,2]
  return(gsd_df)
}
```

```{r, fig.height = 8, fig.width = 4}
env1 <- raster(as.matrix(read.csv("example_data/seed1_env1_H50_r60.csv", header = FALSE)))
env2 <- raster(as.matrix(read.csv("example_data/seed1_env2_H50_r60.csv", header = FALSE)))
extent(env1) <- c(0,100,-100,0)
extent(env2) <- c(0,100,-100,0)
envstk <- stack(env1, env2)
env1 <- data.frame(raster::rasterToPoints(env1), env = 1)
env2 <- data.frame(raster::rasterToPoints(env2), env = 2)
env <- rbind(env1, env2)

envpc <- RStoolbox::rasterPCA(envstk)
envpc <- envpc$map[[1]]
envpcdf <- data.frame(rasterToPoints(envpc))

p1 <- ggplot(env1) +
  geom_tile(aes(x = x, y = y, fill = layer)) +
  coord_equal() +
  theme_void() + 
  scale_fill_viridis(option = "mako") + 
  facet_wrap(~ env, ncol = 1) + 
  theme(legend.position = "none", strip.text.x = element_blank())

p2 <- ggplot(env2) +
  geom_tile(aes(x = x, y = y, fill = layer)) +
  coord_equal() +
  theme_void() + 
  scale_fill_viridis(option = "mako") + 
  facet_wrap(~ env, ncol = 1) + 
  theme(legend.position = "none", strip.text.x = element_blank())

grid.arrange(p1, p2, ncol = 1)

```

```{r, fig.width=4, fig.height=8}
df <- get_gsd("example_data/mod-K1_phi50_m25_seed1_H50_r60_it-0_t-1000_spp-spp_0.csv")
df$y <- -df$y


p1 <- ggplot() +
  geom_tile(data = env1, aes(x = x, y = y, fill = layer), alpha = 0.5) +
  geom_point(data = df, aes(x, y, fill = z1), colour = rgb(1,1,1,0.5), pch = 21, size = 2) +
  coord_equal() +
  theme_void() + 
  scale_color_viridis(option = "mako") + 
  scale_fill_viridis(option = "mako") +
  theme(legend.position = "none")

p2 <- ggplot() +
  geom_tile(data = env1, aes(x = x, y = y, fill = layer), alpha = 0.5) +
  geom_point(data = df, aes(x, y, fill = z2), colour = rgb(1,1,1,0.5), pch = 21, size = 2) +
  coord_equal() +
  theme_void() + 
  scale_color_viridis(option = "mako") + 
  scale_fill_viridis(option = "mako") +
  theme(legend.position = "none")

grid.arrange(p1, p2, ncol = 1)
```
```{r, fig.width=4, fig.height=8}

subdf <- df[sample(nrow(df), 144), ]
p1 <- ggplot() +
  geom_tile(data = env1, aes(x = x, y = y, fill = layer), alpha = 0.5) +
  geom_point(data = subdf, aes(x, y, fill = z1), colour =rgb(0,0,0,0.5), pch = 21, size = 3) +
  coord_equal() +
  theme_void() + 
  scale_color_viridis(option = "mako") + 
  scale_fill_viridis(option = "mako") +
  theme(legend.position = "none")

p2 <- ggplot() +
  geom_tile(data = env1, aes(x = x, y = y, fill = layer), alpha = 0.5) +
  geom_point(data = subdf, aes(x, y, fill = z2), colour = rgb(0,0,0,0.5), pch = 21, size = 3) +
  coord_equal() +
  theme_void() + 
  scale_color_viridis(option = "mako") + 
  scale_fill_viridis(option = "mako") +
  theme(legend.position = "none")

grid.arrange(p1, p2, ncol = 1)
```

```{r}
vcf_to_dosage <- function(x) {

  # Convert to genlight
  genlight <- vcfR::vcfR2genlight(x)

  # Convert to dosage matrix
  gen <- as.matrix(genlight)

  return(gen)
}

```

```{r}
vcf <- vcfR::read.vcfR("example_data/mod-K1_phi50_m25_seed1_H50_r60_it-0_t-1000_spp-spp_0.vcf")
dos <- vcf_to_dosage(vcf[1:8,])
colnames(dos) <- paste0("locus_", 1:8)
df <- data.frame(df, dos)

```


```{r, fig.width = 16, fig.height = 8}


gd <- 
  subdf %>%
  pivot_longer(paste0("locus_", 1:8), "locus") %>%
  mutate(locus = gsub("locus_", "", locus)) %>%
  mutate(value = value/2)

gd1 <- gd %>% filter(locus %in% c(1,2,3,4))

p1 <- ggplot() +
  geom_tile(data = env1, aes(x = x, y = y, fill = layer), alpha = 0.5) +
  geom_point(data = gd1, aes(x, y, col = value), size = 4) +
  coord_equal() +
  theme_void() + 
  scale_fill_viridis(option = "mako") +
  scale_color_viridis(option = "magma") +
  theme(legend.position = "none", strip.text.x = element_blank()) +
  facet_grid( ~ locus)


gd2 <- gd %>% filter(locus %in% c(5,6,7,8))

p2 <- ggplot() +
  geom_tile(data = env2, aes(x = x, y = y, fill = layer), alpha = 0.5) +
  geom_point(data = gd2, aes(x, y, col = value), size = 4) +
  coord_equal() +
  theme_void() + 
  scale_fill_viridis(option = "mako") +
  scale_color_viridis(option = "magma") +
  theme(legend.position = "none", strip.text.x = element_blank()) +
  facet_grid( ~ locus)


grid.arrange(p1, p2, nrow = 2)
```


```{r, fig.width = 8, fig.height=8}
set.seed(144)
s <- sample(nrow(dos), 144)
gendist <- as.matrix(dist(dos[s,], diag = TRUE, upper = TRUE))
p1 <- gendist  %>%
  data.frame() %>%
  rownames_to_column("sample") %>%
  gather("sample2", "gendist", -"sample") %>%
  ggplot(ggplot2::aes(x = sample, y = sample2, fill = gendist)) +
    geom_tile() +
    coord_equal() +
    scale_fill_viridis(option = "mako") +
    theme_void() +
    theme(legend.position = "none", plot.margin = unit(rep(0.5,4), "cm")) 

envdist1 <- as.matrix(dist(df$env1[s], diag = TRUE, upper = TRUE))
p2 <- envdist1  %>%
  data.frame() %>%
  rownames_to_column("sample") %>%
  gather("sample2", "gendist", -"sample") %>%
  ggplot(ggplot2::aes(x = sample, y = sample2, fill = gendist)) +
    geom_tile() +
    coord_equal() +
    scale_fill_viridis(option = "viridis") +
    theme_void() +
    theme(legend.position = "none", plot.margin = unit(rep(0.5,4), "cm")) 

envdist2 <- as.matrix(dist(df$env2[s], diag = TRUE, upper = TRUE))
p3 <- envdist2 %>%
  data.frame() %>%
  rownames_to_column("sample") %>%
  gather("sample2", "gendist", -"sample") %>%
  ggplot(ggplot2::aes(x = sample, y = sample2, fill = gendist)) +
    geom_tile() +
    coord_equal() +
    scale_fill_viridis(option = "viridis") +
    theme_void() +
    theme(legend.position = "none", plot.margin = unit(rep(0.5,4), "cm")) 

geodist <- as.matrix(dist(df[s,c("x","y")], diag = TRUE, upper = TRUE))
p4 <- geodist %>%
  data.frame() %>%
  rownames_to_column("sample") %>%
  gather("sample2", "gendist", -"sample") %>%
  ggplot(ggplot2::aes(x = sample, y = sample2, fill = gendist)) +
    geom_tile() +
    coord_equal() +
    scale_fill_viridis(option = "viridis") +
    theme_void() +
    theme(legend.position = "none", plot.margin = unit(rep(0.5,4), "cm")) 

grid.arrange(p1,p2,p3,p4,nrow = 2)

p1
```
```{r, include = false}

plot_phenotype2 <- function(df1, df2, env, title1, title2){
  df1$y <- -df1$y
  df2$y <- -df2$y
  
  p1 <- ggplot() +
    geom_tile(data = env, aes(x = x, y = y), fill = "gray", alpha = 0.5) +
    geom_point(data = df1, aes(x, y, col = z1), size = 0.9, alpha = 0.7) +
    coord_equal() +
    theme_void() + 
    scale_color_viridis(option = "viridis", limits = c(0,1)) + 
    theme(legend.position = "none") + 
    ggtitle(title1)
  
  p2 <- ggplot() +
    geom_tile(data = env, aes(x = x, y = y), fill = "gray", alpha = 0.5) +
    geom_point(data = df2, aes(x, y, col = z1), size = 0.9, alpha = 0.7) +
    coord_equal() +
    theme_void() + 
    scale_color_viridis(option = "viridis", limits = c(0,1)) + 
    theme(legend.position = "none") + 
    ggtitle(title2)
  grid.arrange(p1, p2, nrow = 1)
}

dfs <- purrr::map(list.files("example_data/example_combos", full.names = TRUE), get_gsd)
names(dfs) <- c("phi10", "H5", "r30", "D", "m25", "K2")

K <- plot_phenotype2(dfs$D, dfs$K2, env1, title1 = "Small Population", title2 = "Large Population")
phi <- plot_phenotype2(dfs$phi10, dfs$D, env1, title1 = "Weak Selection", title2 = "Strong Selection")
m <- plot_phenotype2(dfs$m25, dfs$D, env1, title1 = "Low Migration", title2 = "High Migration")
```
```{r, fig.width = 6, fig.height= 9}
grid.arrange(K, phi, m, nrow = 3)
```

```{r, fig.height=3, fig.width=6}

raster_to_gg <- function(x){
  x <- raster(as.matrix(read.csv(x, header = FALSE)))
  extent(x) <- c(0,100,-100,0)
  x <- data.frame(raster::rasterToPoints(x))
  return(x)
}

plot_env <- function(x, title = ""){
  ggplot(x) +
    geom_tile(aes(x = x, y = y, fill = layer)) +
    coord_equal() +
    theme_void() + 
    scale_fill_viridis(option = "viridis") + 
    theme(legend.position = "none", strip.text.x = element_blank()) + 
    ggtitle(title)
}


env1_H50_r60 <- raster_to_gg("example_data/seed1_env1_H50_r60.csv")
env1_H50_r30 <- raster_to_gg("example_data/seed1_env1_H50_r30.csv")
env1_H5_r60 <- raster_to_gg("example_data/seed1_env1_H5_r60.csv")
env2_H50_r60 <- raster_to_gg("example_data/seed1_env2_H50_r60.csv")
env2_H50_r30 <- raster_to_gg("example_data/seed1_env2_H50_r30.csv")

env1_H50_r60p <- plot_env(env1_H50_r60, title = "Strong Autocorrelation")
env1_H5_r60p <- plot_env(env1_H5_r60, title = "Weak Autocorrelation")

env1_H50_r60pr <- plot_env(env1_H50_r60, title = "Strong Correlation")
env2_H50_r60p <- plot_env(env2_H50_r60)
env2_H50_r30p <- plot_env(env2_H50_r30)
env1_H50_r30p <- plot_env(env1_H50_r30, title = "Weak Correlation")

grid.arrange(env1_H50_r60p, env1_H5_r60p, nrow = 1)

```
```{r, fig.height = 6, fig.width = 6}
g1 <- grid.arrange(env1_H50_r60pr, env2_H50_r60p, ncol = 1)
g2 <- grid.arrange(env1_H50_r30p, env2_H50_r30p, ncol = 1)
grid.arrange(g1,g2,ncol =2)
```

```{r}
get_samples <- function(param_set, sampstrat, nsamp, dir = "example_data/samples", site = FALSE){
  #param_set - vector of one set of parameters (e.g. params[i,])
  #sampstrat - sampling strategy (e.g. "rand", "grid", "trans", "envgeo")
  #nsamp - number of samples

  if(!site) subIDs <- read.csv(paste0(dir, "/samples_", sampstrat, nsamp, ".csv"))
  if(site) subIDs <- read.csv(paste0(dir, "/site_samples_", sampstrat, nsamp, ".csv"))
  
  subIDs <- subIDs[subIDs$K == param_set$K 
                   & subIDs$phi == param_set$phi
                   & subIDs$m == param_set$m 
                   & subIDs$seed == param_set$seed
                   & subIDs$H == param_set$H
                   & subIDs$r == param_set$r
                   & subIDs$it == param_set$it,]
  
  #confirm there is only one set of IDs being used
  stopifnot(nrow(subIDs) == 1)
  
  #remove parameter columns and convert to vector of IDs
  subIDs <- subIDs[,!names(subIDs) %in% names(param_set)]
  subIDs <- unlist(subIDs)
  
  #confirm that final set of IDs is a vector
  stopifnot(is.vector(subIDs))
  
  return(as.character(subIDs))
}
```
```{r}

```

```{r}



plot_samples <- function(.x, .y, site = FALSE){
  #subsample from data based on sampling strategy and number of samples
  if(site) subIDs <- get_samples(param_set, sampstrat = .x, nsamp = 16, site = TRUE)
  if(!site) subIDs <- get_samples(param_set, sampstrat = .x, nsamp = 81, site = FALSE)
  subgsd_df <- df[subIDs,]
  
  p <- ggplot() +
    geom_tile(data = env1, aes(x = x, y = y, fill = layer), alpha = 0.5) +
    geom_point(data = subgsd_df, aes(x, -y, fill = z1), colour = rgb(0,0,0,0.5), pch = 21, size = 3) +
    coord_equal() +
    theme_void() + 
    scale_color_viridis(option = "viridis") + 
    scale_fill_viridis(option = "viridis") +
    theme(legend.position = "none") +
    ggtitle(.y)
  
  return(p)
}

nsite = 16
df <- get_gsd("example_data/example_combos/mod-K1_phi50_m100_seed1_H50_r60_it-0_t-1000_spp-spp_0.csv")

param_set <- data.frame(K = 1, phi = 0.50, m = 1, seed = 1, H = 0.50, r = 0.60, it = 0)

grd_site <- map2(c("rand", "equi", "envgeo"), c("Random", "Equidistant", "EGS"), 
                 plot_samples, site = TRUE)

grd_ind <- map2(c("rand", "grid", "envgeo", "trans"), 
               c("Random", "Grid", "EGS", "Transect"), plot_samples, site = FALSE)
```
```{r, fig.height = 3, fig.width = 9}
do.call(grid.arrange, c(grd_site, nrow = 1))
```

```{r, fig.height = 3, fig.width = 12}
do.call(grid.arrange, c(grd_ind, nrow = 1))
```
```{r, fig.width = 3, fig.height = 3}

names(envpc) <- "layer"
plot_env(envpc)
```

# IGNORE
```{r}
library(algatr)
vcf <- vcfR::read.vcfR("example_data/mod-K2_phi50_m100_seed1_H50_r60_it-0_t-1000_spp-spp_0.vcf")
df <- get_gsd("example_data/mod-K2_phi50_m100_seed1_H50_r60_it-0_t-1000_spp-spp_0.csv")
df$y <- -df$y
s <- sample(nrow(df), 36)

env1 <- raster(as.matrix(read.csv("example_data/seed1_env1_H50_r60.csv", header = FALSE)))
env2 <- raster(as.matrix(read.csv("example_data/seed1_env2_H50_r60.csv", header = FALSE)))
extent(env1) <- c(0,100,-100,0)
extent(env2) <- c(0,100,-100,0)
envstk <- stack(env1, env2)

gen <- vcf_to_dosage(vcf[,c(1, s + 1)])
Ktw <- algatr::select_K_tw(gen, criticalpoint = 2.0234) 

Kscree <- algatr::select_K_elbow(gen, low = 0.08, max.pc = 0.70) 

Kfc <- select_K_fc(gen, pca.select = "percVar", perc.pca = 90, choose.n.clust = FALSE, criterion = NULL, max.n.clust = 10)

pc <- prcomp(gen)
pc <- pc$sdev^2
plot(pc, xlim = c(0,30))
abline(v = Ktw, col = "red")
abline(v = Kscree, col = "blue")
abline(v = Kfc, col = "green")

lfmm_res <- lfmm_do_everything(vcf[,c(1, s + 1)], 
                               coords = df[s, c("x","y")], 
                               env = envstk, K = 10)

rda_res <- rda_do_everything(vcf[,c(1, s + 1)], 
                             coords = df[s, c("x","y")],
                             env = envstk,
                             model = "full")
```


